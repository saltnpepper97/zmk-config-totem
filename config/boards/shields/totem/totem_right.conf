CONFIG_BT_BAS=y
# This config's peripheral reporter samples the ADC itself. Disable ZMK's
# competing cached BAS writer, which cannot distinguish a real 0% from the
# split central's disconnected sentinel.
CONFIG_ZMK_BATTERY_REPORTING=n
CONFIG_SENSOR=y
CONFIG_BT_CTLR_TX_PWR_PLUS_8=y
